@model Hadia.Models.ViewModels.SpouseEducationMasterViewModel
@{
    ViewData["Title"] = "Spouse Education";
    ViewData["Description"] = "Create";
}

<form asp-action="Create" asp-controller="SpouseEducation" method="post">
    <div class="card">
        <div class="card-header header-elements-inline">
            <h5 class="card-title"></h5>
            <div class="header-elements">
            </div>
        </div>

        <div class="card-body">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class=" form-group">
                <label asp-for="QualificationName" class="control-label"></label>
                <Input asp-for="QualificationName" class="form-control" />
                <span asp-validation-for="QualificationName" class="text-danger"></span>
            </div>

        </div>
        <div class="card-footer d-flex justify-content-between align-items-center bg-white">
            <a asp-action="Index" class="btn btn-light">Cancel</a>
            <div class="d-inline-flex">
                <button type="reset" name="SaveNew"  value="SaveNew"  class="btn btn-warning">Save & New</button>
                <button type="submit" name="btnSave"  value="Save" class="btn bg-blue ml-3">Save <i class="icon-paperplane ml-2"></i></button>
            </div>
        </div>
    </div>

</form>
@section Scripts
    {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}